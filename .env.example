# Local development settings.
# Copy to .env when the application configuration exists.

APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000
DATABASE_URL=sqlite:///data/outreach.db
CSRF_SECRET=replace-with-a-local-random-string
MAX_PDF_SIZE_MB=25
